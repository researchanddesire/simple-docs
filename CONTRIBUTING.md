# Contributing

Edit English `.mdx` pages and their assets under `content/`, then open a PR into
`staging`. Use `meta.json` for navigation. Include enough context to verify
hardware instructions. Preserve licenses, attribution, and BOM provenance.

No local build, translation step, image compressor, or GitHub Action is needed.
rad-app handles validation and processing when the hub reconciles the content.
